const name = document.getElementById('email')
const form-group = document.getElementById('form')


form-group.addEventListener('submit', (e) => {
  let messages = []
  if (email.value === '' || email.value == null) {
    messages.push('Name is required')
  }
  
}
)